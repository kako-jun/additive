# additive-13 web

The standalone web app for ADDITIVE-13 — Astro + Solid + Tailwind, deployed to
Cloudflare Pages at `additive-13.llll-ll.com`.

**Status:** placeholder. The Studio GUI is implemented in issue #5. The build
wiring (wasm-pack → `src/wasm/`, Astro static output, wrangler Pages) mirrors
orber's `web/`. See `../DESIGN.md` for the visual language.
